<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.kibotu.android.recyclerviewpresenter.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".java.PresenterActivity" />
        <activity android:name=".screens.kotlin.PresenterActivity" />
        <activity android:name=".screens.pagination.PaginationActivity" />
        <activity android:name=".screens.v2.V2Activity" />
        <activity android:name=".screens.nested.NestedActivity" />
        <activity android:name=".screens.listadapter.ListPresenterActivity" />
    </application>

</manifest>